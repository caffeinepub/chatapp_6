{
  "frontend/src/index.css": "Global styles with OKLCH dark/light color system, Inter font import, chat-specific CSS tokens (bubble-sent, bubble-received, chat-sidebar), and scrollbar utilities.",
  "frontend/tailwind.config.js": "Tailwind config extended with Inter/JetBrains Mono fonts, chat shadow, fade-in/slide-in animations, and all OKLCH color tokens.",
  "frontend/src/hooks/useQueries.ts": "React Query hooks for all backend operations — exports backend types (`UserProfile`, `Project`, etc.) from `../backend`, defines local chat UI types (`ChatUser`, `Message`, `Conversation`) for the chat layer, and provides both the correct backend-wired profile hooks (`useGetCallerUserProfile`, `useSaveCallerUserProfile`) and the chat UI hooks (`useGetCurrentUser`, `useGetUsers`, `useGetMyConversations`, `useGetConversation`, `useSendMessage`) plus project/workflow/task CRUD hooks.",
  "frontend/src/App.tsx": "Root app component that uses the corrected `useGetCallerUserProfile` hook (with the flash-prevention `isFetched` guard from the authorization component docs) to route between LoginPage, SetupPage, and ChatPage, and invalidates the profile query on setup completion instead of relying on a local `setupComplete` flag.",
  "frontend/src/pages/LoginPage.tsx": "Full-page login screen with Internet Identity sign-in button, feature highlights, and branded footer with caffeine.ai attribution.",
  "frontend/src/pages/SetupPage.tsx": "First-time user setup page that now calls `useSaveCallerUserProfile` (passing `{ name: trimmed }`) instead of the broken `useRegisterUser`, with inline error display distinguishing network errors, canister rejections, and validation issues, and full error logging to the console.",
  "frontend/src/pages/ChatPage.tsx": "Main chat layout with responsive two-panel design (conversation list sidebar + message thread) — updated to import `ChatUser` instead of `UserProfile` from `useQueries` and use `ChatUser` for the `selectedUser` state type.",
  "frontend/src/components/ChatHeader.tsx": "App header with logo, theme toggle, and user dropdown — updated to import `ChatUser` (instead of `UserProfile`) from `useQueries` and use `ChatUser` for the `currentUser` prop type.",
  "frontend/src/components/ConversationList.tsx": "Left sidebar showing conversation list with search, skeleton loading, empty states, and a New Chat dialog — updated to import `ChatUser` and `Conversation` (instead of `UserProfile`) from `useQueries` and use `ChatUser` throughout all prop types and internal logic.",
  "frontend/src/components/MessageThread.tsx": "Right panel showing message bubbles grouped by date, auto-scroll, send input with Enter-to-send, and loading/empty states — updated to import `ChatUser` and `Message` (instead of `UserProfile`) from `useQueries` and use `ChatUser` throughout all prop types and internal logic.",
  "frontend/src/lib/utils.ts": "Utility function cn() combining clsx and tailwind-merge for conditional class names."
}